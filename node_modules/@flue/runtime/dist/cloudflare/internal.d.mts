import { o as ExtensionClass, p as runWithCloudflareContext, r as installDefaultCloudflareTracing } from "../index-BTs049o5.mjs";
import { r as CloudflareAgentRuntime } from "../agent-coordinator-DN97gQxN.mjs";
import { n as CloudflareRuntime } from "../flue-app-p6KE9Aed.mjs";
//#region src/cloudflare/flue-agent-class.d.ts
interface CreateFlueAgentClassOptions {
  /**
   * The Cloudflare Agents SDK `Agent` class (the generated entry imports it
   * from the user's `agents` package; `@flue/runtime` does not depend on it).
   */
  readonly AgentBase: ExtensionClass<any>;
  /** The shared per-Worker Cloudflare agent runtime (`createCloudflareAgentRuntime`). */
  readonly runtime: CloudflareAgentRuntime;
  /** Generated Durable Object class name, e.g. `FlueTriageAgent`. */
  readonly className: string;
  /** The agent's identity (file basename), e.g. `triage`. */
  readonly agentName: string;
  /**
   * The agent module's `cloudflare` named export, if any — must be created
   * with `extend({ base, wrap })` from `@flue/runtime/cloudflare`.
   */
  readonly extension?: unknown;
}
/**
 * Build the final (possibly extension-wrapped) Durable Object class for one
 * agent module.
 */
declare function createFlueAgentClass(options: CreateFlueAgentClassOptions): ExtensionClass<any>;
//#endregion
//#region src/cloudflare/worker-config.d.ts
/** How the generated entry addresses one scanned agent's Durable Object. */
interface CloudflareAgentIdentity {
  readonly bindingName: string;
  readonly className: string;
}
interface CreateCloudflareWorkerConfigOptions {
  /**
   * Module-scope `env` from `cloudflare:workers` — the binding source for
   * calls that carry no per-request env (cron callbacks, queue consumers,
   * Workflow steps, and the programmatic agent client).
   */
  env: unknown;
  /** Agent identity → Durable Object binding, from the build-time scan. */
  agentIdentities: Record<string, CloudflareAgentIdentity>;
  /** Route one request to the named instance of an agent DO binding. */
  fetchAgent: (binding: unknown, instanceId: string, request: Request) => Promise<Response>;
}
/** The Cloudflare-target seams the generated entry passes to `configureFlueRuntime`. */
type CloudflareWorkerConfig = Pick<CloudflareRuntime, 'dispatchQueue' | 'routeAgentRequest' | 'instanceInfo'>;
declare function createCloudflareWorkerConfig(options: CreateCloudflareWorkerConfigOptions): CloudflareWorkerConfig;
//#endregion
export { type CloudflareAgentIdentity, type CloudflareWorkerConfig, type CreateCloudflareWorkerConfigOptions, type CreateFlueAgentClassOptions, createCloudflareWorkerConfig, createFlueAgentClass, installDefaultCloudflareTracing, runWithCloudflareContext };