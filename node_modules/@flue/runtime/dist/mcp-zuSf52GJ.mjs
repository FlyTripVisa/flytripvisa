import { p as registerPreparedToolAdapter } from "./result-DfjetCf9.mjs";
import { Client, SSEClientTransport, StreamableHTTPClientTransport } from "@modelcontextprotocol/client";
//#region package.json
var version = "2.0.3";
//#endregion
//#region src/mcp.ts
/**
* A per-instance MCP connection cache: the first declaration of a server
* name connects; later submissions reuse the live connection for the
* instance's in-memory lifetime, so definitions are read at first connect
* (an `auth` resolver stays per-request). Concurrent resolves of one name
* share a single in-flight connect. A failed connect is evicted immediately —
* a transient outage must not brick the instance, so the next submission
* retries with a freshly read definition.
*/
function createMcpConnectionCache() {
	const connections = /* @__PURE__ */ new Map();
	return {
		resolve(definition) {
			const cached = connections.get(definition.name);
			if (cached) return cached;
			const pending = createMcpConnection(definition);
			connections.set(definition.name, pending);
			pending.catch(() => {
				if (connections.get(definition.name) === pending) connections.delete(definition.name);
			});
			return pending;
		},
		async close() {
			const pending = [...connections.values()];
			connections.clear();
			await Promise.allSettled(pending.map(async (connection) => (await connection).close()));
		}
	};
}
/**
* Connects to a remote MCP server described by a
* {@link McpConnectionDefinition} and adapts its listed tools into ordinary
* Flue tool definitions.
*
* Adapted tool names use `mcp__<server>__<tool>`. Unsupported characters are
* replaced with underscores, and duplicate adapted names are rejected. Close
* the returned connection when its tools are no longer needed.
*/
async function createMcpConnection(definition) {
	const url = definition.url instanceof URL ? definition.url : new URL(definition.url);
	const requestInit = mergeRequestInit(definition.requestInit, definition.headers);
	const transport = createTransport(url, definition.transport ?? "streamable-http", requestInit, definition.fetch, definition.auth === void 0 ? void 0 : createAuthProvider(definition.auth));
	const client = new Client({
		name: "flue",
		version
	});
	return createMcpConnectionWithClient(definition.name, client, transport, {
		timeout: definition.timeoutMs,
		resetTimeoutOnProgress: definition.resetTimeoutOnProgress
	}, { tools: definition.tools });
}
async function createMcpConnectionWithClient(name, client, transport, requestOptions = {}, selection = {}) {
	try {
		await client.connect(transport);
		let page = await client.listTools(void 0, requestOptions);
		const tools = [...page.tools];
		const seenCursors = /* @__PURE__ */ new Set();
		while (page.nextCursor !== void 0) {
			if (seenCursors.has(page.nextCursor)) throw new Error(`[flue] MCP server "${name}" repeated tools/list cursor ${JSON.stringify(page.nextCursor)} during tool discovery.`);
			seenCursors.add(page.nextCursor);
			page = await client.listTools({ cursor: page.nextCursor }, requestOptions);
			tools.push(...page.tools);
		}
		return {
			name,
			tools: createMcpTools(name, client, selectMcpTools(name, tools, selection.tools), requestOptions),
			close: () => client.close()
		};
	} catch (error) {
		await client.close().catch(() => void 0);
		throw error;
	}
}
/**
* Adapt the `auth` credential to the MCP SDK's {@link AuthProvider}: the
* transport calls `token()` before every request, and on a 401 awaits
* `onUnauthorized` and retries once — re-resolving the token, so the
* application's credential store is the refresh policy.
*/
function createAuthProvider(auth) {
	const resolveToken = typeof auth === "function" ? auth : () => auth;
	return {
		token: async () => resolveToken(),
		onUnauthorized: async () => {}
	};
}
/**
* Apply the `tools` allowlist to the discovered listing, in allowlist order.
* Every allowlisted name must exist and be callable — a typo or an
* unsupported tool must fail loud, not silently narrow the tool set.
*/
function selectMcpTools(serverName, discovered, allowlist) {
	if (allowlist === void 0) return discovered;
	const byName = new Map(discovered.map((tool) => [tool.name, tool]));
	const duplicates = allowlist.filter((name, index) => allowlist.indexOf(name) !== index);
	if (duplicates.length > 0) throw new Error(`[flue] MCP server "${serverName}" tools allowlist repeats ${formatToolNames(duplicates)}.`);
	const unknown = allowlist.filter((name) => !byName.has(name));
	if (unknown.length > 0) throw new Error(`[flue] MCP server "${serverName}" does not expose ${formatToolNames(unknown)} named in the tools allowlist. Discovered tools: ${discovered.map((tool) => tool.name).join(", ") || "(none)"}.`);
	return allowlist.map((name) => {
		const tool = byName.get(name);
		if (tool.execution?.taskSupport === "required") throw new Error(`[flue] MCP tool "${name}" from server "${serverName}" requires task-based execution, which is not supported — remove it from the tools allowlist.`);
		return tool;
	});
}
function formatToolNames(names) {
	return [...new Set(names)].map((name) => JSON.stringify(name)).join(", ");
}
function createTransport(url, transport, requestInit, fetchImpl, authProvider) {
	if (transport === "sse") return new SSEClientTransport(url, {
		requestInit,
		fetch: fetchImpl,
		authProvider
	});
	return new StreamableHTTPClientTransport(url, {
		requestInit,
		fetch: fetchImpl,
		authProvider
	});
}
function createMcpTools(serverName, client, tools, requestOptions) {
	const names = /* @__PURE__ */ new Set();
	return tools.filter((tool) => {
		if (tool.execution?.taskSupport !== "required") return true;
		console.warn(`[flue] Skipping MCP tool "${tool.name}" from server "${serverName}": it requires task-based execution, which is not supported.`);
		return false;
	}).map((tool) => {
		const toolName = createToolName(serverName, tool.name);
		if (names.has(toolName)) throw new Error(`[flue] MCP tools from server "${serverName}" produced duplicate tool name "${toolName}".`);
		names.add(toolName);
		const definition = {
			name: toolName,
			description: createToolDescription(serverName, tool),
			input: void 0,
			output: void 0,
			run() {
				throw new Error("[flue] MCP tools execute through the internal adapter.");
			}
		};
		registerPreparedToolAdapter(definition, {
			parameters: normalizeInputSchema(tool.inputSchema),
			async execute(args, signal) {
				if (signal?.aborted) throw new Error("Operation aborted");
				const result = await client.callTool({
					name: tool.name,
					arguments: args
				}, {
					...requestOptions,
					signal
				});
				const text = formatMcpResult(result);
				if (result.isError) throw new Error(text);
				return text;
			}
		});
		return Object.freeze(definition);
	});
}
function mergeRequestInit(requestInit, headers) {
	if (!headers) return requestInit ?? {};
	const mergedHeaders = new Headers(requestInit?.headers);
	for (const [key, value] of new Headers(headers)) mergedHeaders.set(key, value);
	return {
		...requestInit,
		headers: mergedHeaders
	};
}
function createToolName(serverName, toolName) {
	return `mcp__${sanitizeToolNamePart(serverName)}__${sanitizeToolNamePart(toolName)}`;
}
function sanitizeToolNamePart(value) {
	return value.replace(/[^A-Za-z0-9_-]/g, "_").replace(/^_+|_+$/g, "") || "unnamed";
}
function createToolDescription(serverName, tool) {
	const parts = [];
	if (sanitizeToolNamePart(serverName) !== serverName || sanitizeToolNamePart(tool.name) !== tool.name) parts.push(`MCP tool "${tool.name}" from server "${serverName}".`);
	const title = tool.title ?? tool.annotations?.title;
	if (title && title !== tool.name) parts.push(`Title: ${title}.`);
	if (tool.description) parts.push(tool.description);
	if (parts.length === 0) parts.push(`MCP tool "${tool.name}" from server "${serverName}".`);
	return parts.join(" ");
}
function normalizeInputSchema(schema) {
	return {
		...schema,
		type: schema.type ?? "object",
		properties: schema.properties ?? {},
		required: schema.required
	};
}
function formatMcpResult(result) {
	const parts = [];
	if (result.structuredContent !== void 0) parts.push(`Structured content:\n${JSON.stringify(result.structuredContent, null, 2)}`);
	for (const item of result.content ?? []) {
		if (item.type === "text") {
			parts.push(item.text);
			continue;
		}
		if (item.type === "image") {
			parts.push(`[Image: ${item.mimeType}, ${item.data.length} base64 chars]`);
			continue;
		}
		if (item.type === "audio") {
			parts.push(`[Audio: ${item.mimeType}, ${item.data.length} base64 chars]`);
			continue;
		}
		if (item.type === "resource") {
			const resource = item.resource;
			if ("text" in resource) parts.push(`[Resource: ${resource.uri}]\n${resource.text}`);
			else parts.push(`[Resource: ${resource.uri}, ${resource.blob.length} base64 chars]`);
			continue;
		}
		if (item.type === "resource_link") {
			const description = item.description ? ` - ${item.description}` : "";
			parts.push(`[Resource link: ${item.name} (${item.uri})${description}]`);
			continue;
		}
		parts.push(JSON.stringify(item));
	}
	return parts.filter(Boolean).join("\n\n") || "(MCP tool returned no content)";
}
//#endregion
export { createMcpConnectionCache as n, createMcpConnection as t };
