//#region src/provider-diagnostics.ts
/** Diagnostic `type` under which providers attach response metadata. */
const PROVIDER_RESPONSE_DIAGNOSTIC = "flue:provider_response";
/**
* Attach (or update) the message's provider-response diagnostic. Values merge
* over an existing entry so a provider can record fields as they arrive —
* the gateway header when the response lands, the finish reason when the
* stream delivers it — and the entry survives onto error-path messages too.
*/
function attachProviderResponseDiagnostics(message, details) {
	message.diagnostics ??= [];
	const diagnostics = message.diagnostics;
	const existing = diagnostics.find((diagnostic) => diagnostic.type === PROVIDER_RESPONSE_DIAGNOSTIC);
	if (existing) {
		existing.details = {
			...existing.details,
			...pickAllowlisted(details)
		};
		return;
	}
	diagnostics.push({
		type: PROVIDER_RESPONSE_DIAGNOSTIC,
		timestamp: Date.now(),
		details: pickAllowlisted(details)
	});
}
/**
* Read the allowlisted provider-response metadata off a message. Unknown
* fields in the diagnostic's `details` are deliberately not returned.
*/
function readProviderResponseDiagnostics(message) {
	const details = message.diagnostics?.findLast((diagnostic) => diagnostic.type === PROVIDER_RESPONSE_DIAGNOSTIC)?.details;
	if (!details) return void 0;
	const picked = pickAllowlisted(details);
	return Object.keys(picked).length > 0 ? picked : void 0;
}
function pickAllowlisted(details) {
	return {
		...typeof details.providerFinishReason === "string" && details.providerFinishReason.length > 0 ? { providerFinishReason: details.providerFinishReason } : {},
		...typeof details.gatewayLogId === "string" && details.gatewayLogId.length > 0 ? { gatewayLogId: details.gatewayLogId } : {}
	};
}
//#endregion
export { readProviderResponseDiagnostics as n, attachProviderResponseDiagnostics as t };
