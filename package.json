{
  "name": "flytripvisa",
  "version": "1.0.0",
  "description": "FlyTripVisa - AI-powered visa platform (Cloudflare D1 + Worker)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute flytripvisa_db --file=schema.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
